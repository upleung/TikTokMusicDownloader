下面是为你的仓库 **TikTokMusicDownloader** 编写的 **需求文档.md**（完全基于你上传的资料.txt，并结合你真实的目标：一键抓取下载 TikTok 收藏音乐列表 423 首）。  
内容结构清晰、可直接放进 GitHub 仓库作为正式需求文档。

---

# TikTokMusicDownloader · 需求文档  
**版本：v1.0**  
**作者：NetX**  
**目标：实现一个真正可“一键抓取下载 TikTok 收藏音乐列表”的工具**

---

## 🎯 1. 项目目标（核心需求）

本项目旨在实现：

> **TikTok 登录账号 → 自动读取「收藏音乐列表」→ 自动导出全部音乐信息 → 自动批量下载音频 → 自动去重 → 自动断点续传 → 一键运行**

适用于用户收藏数量较大的场景（如 423 首音乐），避免手工逐条下载。

---

## 📌 2. 背景说明（来自资料.txt 的关键结论）

根据资料.txt 的分析：

- TikTok 的 **「音乐 423」** 是账号的 **收藏音乐列表（Music Favorites）**，不是视频背景音乐。
- 现有项目（如 TikTokDownloader）历史上确实支持过“收藏音乐下载”，但：
  - 最新版本已重构
  - 加密参数算法过期
  - 功能不再可靠
- 因此 **不建议依赖旧项目或修改现有项目**。

资料.txt 推荐的最稳路线：

```
Chrome 登录 TikTok
↓
自动读取 Music Favorites
↓
导出全部音乐 URL/ID
↓
yt-dlp / FFmpeg
↓
批量下载
```

本项目将完全采用这一路线。

---

## 🧩 3. 功能需求（Functional Requirements）

### ### 3.1 收藏音乐抓取模块（Collector）

**功能：自动从 TikTok 网页端读取收藏音乐列表**

要求：

- 自动检测用户是否已在 Chrome 登录 TikTok
- 自动打开收藏音乐页面：  
  `https://www.tiktok.com/@<username>/music`
- 自动滚动页面 / 分页加载全部音乐（例如 423 首）
- 自动提取每首音乐的：
  - music_id
  - title
  - artist
  - cover_url
  - play_url（如果有）
  - 关联视频 ID（作为备用下载源）
- 自动保存到：
  - `data/music.json`
  - `data/music.csv`
  - `data/music.db`（SQLite）

### 3.2 下载模块（Downloader）

**功能：批量下载所有音乐音频文件**

要求：

- 优先使用音乐的 `play_url`
- 若无独立音频：
  - 自动使用关联视频 URL
  - 使用 yt-dlp 提取音频
- 下载格式：
  - 默认：`.m4a`
  - 可选：自动转码为 `.mp3`（FFmpeg）
- 自动生成文件名：
  ```
  0001 - VELUX.m4a
  0002 - Dream_Comee_True.m4a
  ...
  ```
- 自动写入 ID3 标签（title / artist / album / comment）
- 自动断点续传
- 自动失败重试
- 自动跳过已下载文件

### 3.3 去重与数据库（Deduplication）

**功能：确保不会重复下载**

要求：

- 使用 SQLite 记录每首音乐的下载状态
- 若 TikTok 修改音乐标题，不影响去重
- 若用户新增收藏音乐（例如从 423 → 431）：
  - 自动识别新增部分
  - 仅下载新增音乐

### 3.4 一键运行（One-Click Execution）

**功能：用户只需双击一个文件即可完成所有操作**

要求：

- 提供 `start.bat`（Windows）
- 自动执行：
  ```
  ① Collector → 抓取收藏音乐
  ② Dedup → 去重
  ③ Downloader → 下载
  ```
- 输出最终结果：
  ```
  Music/
    0001 - xxx.m4a
    0002 - xxx.m4a
    ...
  data/music.json
  data/music.csv
  data/music.db
  download.log
  ```

---

## 🛠 4. 技术需求（Technical Requirements）

### 4.1 环境要求

- Windows 10/11
- Python ≥ 3.12
- Chrome 浏览器
- FFmpeg（可选）
- yt-dlp

### 4.2 依赖库

- Playwright / Selenium（用于浏览器自动化）
- requests
- sqlite3
- yt-dlp
- mutagen（写 ID3 标签）
- tqdm（进度条）

---

## 🔐 5. Cookie / 登录机制

- 用户必须在 Chrome 中登录 TikTok
- 程序通过浏览器自动化读取页面，不需要手动复制 Cookie
- 不保存用户密码
- 不上传任何用户数据

---

## 📂 6. 项目目录结构（建议）

```
TikTokMusicDownloader/
│
├── start.bat
├── README.md
├── requirements.txt
│
├── collector.py
├── downloader.py
├── database.py
├── utils.py
│
├── data/
│   ├── music.json
│   ├── music.csv
│   └── music.db
│
└── Music/
    ├── 0001 - VELUX.m4a
    ├── 0002 - Dream_Comee_True.m4a
    └── ...
```

---

## 🚀 7. 工作流程（Workflow）

```
用户双击 start.bat
↓
程序检测 Chrome 登录状态
↓
自动打开 TikTok 收藏音乐页面
↓
自动滚动加载全部音乐（423 首）
↓
提取音乐信息 → 保存 JSON/CSV/DB
↓
检查本地已下载文件
↓
下载新增音乐
↓
写入 ID3 标签
↓
完成
```

---

## ⚠️ 8. 限制与注意事项

- TikTok 网页结构可能更新，需要适配
- 部分音乐可能没有独立音频 URL，需要从关联视频提取
- 下载行为需遵守 TikTok 使用条款与版权要求
- 本工具仅用于个人备份，不用于传播或商业用途

---

## 📌 9. 未来扩展（Optional）

- Docker 版本（适配 ARMv7 / ARM64）
- 自动封面下载
- 自动生成播放列表（m3u）
- Web UI 控制面板
- 多账号支持

---

## 🏁 10. 最终目标总结

> **打造一个真正可用、稳定、可维护的 TikTok 收藏音乐批量下载工具，支持一键抓取、自动下载、自动去重、断点续传。**

---

如果你愿意，我可以继续为你编写：

✅ `README.md`  
✅ `start.bat`  
✅ `collector.py`（可直接运行）  
✅ `downloader.py`  
✅ `database.py`  
✅ 完整项目骨架  
✅ 自动化滚动抓取收藏音乐的 Playwright 代码  

你想让我继续生成哪一部分？
